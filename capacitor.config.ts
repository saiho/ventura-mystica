import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saiho.venturamystica',
  appName: 'Ventura Mystica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
